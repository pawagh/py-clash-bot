"""Integration tools that talk to a live emulator.

Each module in here is runnable as a script, e.g.

    uv run python -m rl.tools.calibrate
    uv run python -m rl.tools.random_policy --episodes 2

These require BlueStacks (macOS/Windows) or MEmu (Windows) to be running
with Clash Royale installed and logged in.
"""
